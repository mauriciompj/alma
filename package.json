{
  "name": "alma-app",
  "version": "3.0.0",
  "description": "ALMA — A Voz do Pai. Sistema de legado emocional paternal com RAG + Neon DB.",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step needed'",
    "seed": "node db/run-seed.mjs",
    "seed:demo": "node db/run-seed-demo.mjs",
    "backup": "node db/backup.mjs",
    "test": "node tests/auth.test.mjs",
    "test:prod": "TEST_URL=https://projeto-alma.netlify.app node tests/auth.test.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "bcryptjs": "^2.4.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "dotenv": "^17.3.1"
  }
}
